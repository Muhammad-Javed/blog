<%= form_for @post do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@post.errors.count, "error") %> prohibited
        this post from being saved:
      </h2>
      <ul>
        <% @post.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.label :title%><br>
  <%= f.text_field :title, minlength: 5, maxlength:30, :required => true %><br>
  <%= f.label :body%><br>
  <%= f.text_area :body, minlength: 10, maxlength:255, :required => true %><br>
  <%= f.submit%>
<% end %>